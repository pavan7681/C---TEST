#include<stdio.h>
int main() {
	int a,b;
	printf("enter a and b values :");
	scanf("%d %d",&a,&b);
	if (a >= b) {
		printf("\na is greater than or equal to b");
	}
	else {
		printf("\nb is greater than a");
	}
	if (a != b) {
		printf("\na is not equal to b");
	}
	//some operations
	if (a >= 5) {
		if (a != 2) {
			a == 0;
		}
	}
	printf("\n%d",a);
}
